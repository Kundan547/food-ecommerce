version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80" # Map port 8080 on your machine to port 80 in the container
    volumes:
      - ./:/usr/share/nginx/html # For hot-reloading during development
    restart: always

